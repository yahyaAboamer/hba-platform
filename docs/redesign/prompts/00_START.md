# First prompt — baseline only

I own the HBA affiliate platform and want the approved complete redesign implemented in its existing repository. I have placed the handoff package at `docs/redesign/`. You start with no prior conversation context: this folder supplies the current business decisions, final design sources, code audit, phase prompts and acceptance requirements.

Start **Phase 00 only**. Do not start rebuilding the app yet.

1. Read applicable repository instructions (including CLAUDE.md/AGENTS.md if present), `docs/redesign/START_HERE.md`, `STATUS.md`, `PRODUCT_RULES.md`, `REPOSITORY_AUDIT.md`, `IMPLEMENTATION_ROADMAP.md` and `DECISIONS.md`. Inspect both final HTML references: Affiliate Portal v3.dc.html for mobile models; Admin Dashboard.dc.html for laptop admin. Older exports and old red/old-navigation/delivered-only/reopen instructions are superseded by the documented latest owner choices, while data/security invariants remain.
2. Inspect the **actual current repository**, not just the handoff's 4 September baseline. Identify branch/SHA, uncommitted user changes, stack, migrations, API/routes, test setup, deployment conditions and any newer implementation. Do not reset or overwrite unrelated changes. The package's author could not authenticate the remote; fix that through the connected environment or report the precise access limit without guessing.
3. Check the handoff hashes (`python docs/redesign/evidence/verify-package.py`), map all UI rows to current code and report differences. Do not read only Products/Models: include the whole portal, finance, targets, identity/onboarding, settings, notices and retained support routes.
4. Establish a local/dev runtime and a **disposable PostgreSQL test database** using the repository's documented setup. Verify database identity before pytest: its fixtures rebuild the schema. Run only one pytest process at a time and never run migrations concurrently against that test database. Use an OS-appropriate Python virtualenv, existing Node requirement/lockfile, and `npm ci`, the frontend tests/build as appropriate. Record actual baseline outcomes; do not copy historical test counts as proof. If infrastructure is unavailable, report it and complete the source audit without claiming runtime verification.
5. Inspect the running old app and design references in a browser if available; capture the key phone/laptop states using synthetic data. If blocked, state the limitation and leave visual acceptance pending; do not claim to have viewed every interaction.
6. Produce `docs/redesign/BASELINE_REPORT.md`: current commit, existing-vs-new changes since 025d8a8, confirmed code reuse/gaps, actual test/runtime results, migration/permissions/data-source constraints, and a refined first implementation batch. Update STATUS.md. Keep large reports free of credentials, actual customer/recipient phones and payment details.
7. Flag only decisions required before the next batch; distinguish owner policy choices from engineering facts you can investigate yourself. Do not re-ask confirmed rules. You may create this report and ordinary local setup needed to inspect/test, but make **no application behavior changes**, Shopify writes/imports, live financial edits, production push/deployment or external invitations/emails in this phase.

Keep React/Vite/FastAPI/PostgreSQL unless actual evidence makes a different approach necessary; explain before proposing a replacement stack. The HTML is a visual reference, not production logic. Preserve server-owned money, immutable approvals/transfers, exact arithmetic and private recipient matching boundaries.

Finish with the baseline report and the next step: Phase 01A. I will review that result and send the next batch prompt. Do not continue into all implementation phases automatically.
