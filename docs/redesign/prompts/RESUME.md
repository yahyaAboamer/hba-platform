# Resume in the same or a new coding-agent session

Continue the HBA redesign in this repository using `docs/redesign/`. Read current repository instructions, `START_HERE.md`, `STATUS.md`, the latest batch report and the current phase prompt. Inspect the current branch/commit and working tree before changing anything. Do not assume the last agent completed a batch because files exist.

Run **only the next unfinished batch named in STATUS.md**, after verifying its prerequisites and accepted decisions. Use the two selected HTML files, canonical PRODUCT_RULES and the matching coverage/acceptance rows. Preserve unrelated work, financial evidence and access rules. Do not re-ask already recorded answers or implement undecided business policy silently.

Complete that batch with real APIs, meaningful checks and a rendered comparison where available. Update STATUS.md and leave a batch report with actual commands/results and the exact next batch. Stop for that batch's review. This instruction does not authorize production deployment or live financial changes.
